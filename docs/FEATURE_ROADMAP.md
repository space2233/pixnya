# PixNya 当前功能与维护路线

> 当前公开稳定版：`1.5.1`
> 当前源码版本：`1.6.5`
> 当前维护范围：`1.6.5`（Publish 工作流跳过过时 Android SDK tools）
> 版本规则：Bug、安全与兼容修复增加第三位；新增功能增加第二位；不兼容迁移才增加第一位

## 已交付功能

- 只读站内通知、评论删除、回复分页和官方贴图评论。
- Ugoira 播放；Windows/Linux 可使用本机 `ffmpeg` 导出 GIF、APNG 和 WebM。
- 官方 access-block、用户静音和标签静音，与本地隐藏/报告明确分区。
- 本地收藏夹、标签、组合筛选、批量整理、重复内容报告和离线资料库。
- 插画与小说官方收藏标签筛选，以及公开性、标签和取消收藏的批量管理。
- 凭据无关的本地数据备份与迁移，可选离线内容，支持合并、替换和崩溃回滚。
- Windows x64/ARM64、Linux x64、Android ARM64/ARM32 的签名 Release 与自动更新。

## 1.4.3 已交付维护内容

- 修复严格 ECH 模式下 Pixiv 图片 CDN 白名单遗漏造成的缩略图失败。
- 统一首次连接选择与设置页视觉，修复未定义主题变量造成的不可见控件。
- 修复小说收藏状态在列表、详情和批量操作之间不同步的问题。
- 补强返回状态、缓存重启/损坏恢复和备份离线目录事务回归。
- 将 Android 构建期 Bouncy Castle 依赖成组升级到 `1.80.2`，消除 Critical 告警并重建供应链证据。
- 统一字号阶梯，精简个人页登录状态，将空间信息迁入存储设置，并修复手机日志操作布局。
- 已在 Windows x64 与 Android ARM64 完成从公开 `1.4.0` 覆盖升级、数据保留、三种连接模式和备份恢复验收。
- Linux x64、Windows ARM64 与 Android ARM32 继续要求 CI 签名构建成功；未完成本轮活体验收时必须在发布记录中说明。

## 1.4.4 已交付维护内容

`1.4.4` 维护工作已完成并打 tag，但从未作为面向用户的 GitHub Release 公开；内容已包含在公开稳定版 `1.5.0` 中。

- 修复异步收藏结果未绑定发起账号和资源的问题，账号切换、退出或清除数据后不再保留旧覆盖状态。
- 修复小说下一页和批量结果跨栏目、筛选或账号写回当前列表的问题。
- 为媒体缓存记录内容 SHA-256，拒绝等长损坏文件持续命中。
- 补齐字号 shorthand 门禁并同步已实施的发布治理状态；该维护版不增加新功能。

## 1.5.0 已交付

- 搜索记录支持 8、20、50、100 条及不限制、内联筛选和单条删除，并随本地备份迁移；不限制仍受 WebView 本机存储配额约束。
- 作品与小说可选“标题与简介”搜索；类别和匹配方式写入 URL，刷新、分享和返回保持一致。
- 浏览历史保持现有 500 条本地数据边界，但按 48 条渐进挂载，降低长列表 DOM 与图片解码压力。
- 统一放大桌面与移动端搜索框清除按钮，补齐浏览历史筛选框。
- 头像和个人背景图优先复用安全域隔离的热缓存，页面往返时立即显示并在后台刷新。
- 在线缓存共用一个总上限；旧原图缓存自动清理，预览图先于缩略图淘汰，设置页显示分类占用。

## 1.5.1 已交付维护内容

`1.5.1` 已公开为 latest stable。固定候选 `302e117e234aff16658c1a6f82c707c1697cd40e`；Draft GitHub Actions `33730303676` 完成五平台构建、签名与 10 附件汇总。该 Release 曾作为 Pre-release 公开；官方 Publish 工作流只接受 Draft，无法再复验，维护者随后直接将其标为 latest。本轮不做人机活体验收。

- 复核并续期 Android 构建工具 OSV 限期例外，使发布闸门在原 2026-09-08 及后续到期日后仍保持精确、限 scope、限版本、限期；runtime 继续零例外。
- 复核仍被 Tauri 2.11.5 锁图挡住的 RustSec 临时例外；不升级 Tauri、不放开全局 ignore。
- 进程内复用磁盘媒体缓存：缩略图/预览命中不再每次 `MediaCache::open`，也不再每次更新 LRU 就写 `index.json`；打开时仍按 1.5.0 策略清除原图缓存，读时仍校验 SHA-256。
- 作品、小说、Ugoira 详情在本地已有对应离线文件时优先使用离线资料库字节；缺失或完整性失败则回退到现有缓存/网络路径。离线资料库与媒体缓存目录保持分离。

## 1.6.0 功能范围

`1.6.0` 已打不可移动的 `v1.6.0` tag，对应首个沉浸小说阅读签名 Draft。公开稳定版仍为 `1.5.1`。当前测试 Draft 是 `1.6.5`。本轮不增加新平台、账号能力或新的缓存接口。

- 小说阅读改为全屏沉浸分页：点中间显示工具栏，左右滑动或点边缘翻页；可调字号、行距和纸色/白底/夜间主题。不提供朗读或阅读页内搜索。
- 设置与阅读门禁的返回控件统一为胶囊形状；关注列表的公开/非公开选项保持单行。
- 2026-09-12 复核 Android 构建工具 OSV：在既有 84 条之外新增 4 条仅 build-only 的 `io.netty:netty-handler` 告警（`GHSA-c4c3-7fpv-j4q5` Critical，`GHSA-fccg-mwvh-qqg4` Moderate）。不升级 Tauri/AGP；ARM runtime 继续零例外。
- 2026-09-12 `cargo-deny` 因 RustSec 撤回 GTK3 停止维护公告而报 unused ignore；从 `deny.toml` 删除 `RUSTSEC-2024-0411`–`0420`，保留 7 条仍命中的例外。
- 在 Windows x64 与 Android ARM64 上，从公开 `1.5.1` 做覆盖升级活体验收；Android 须覆盖标准、ECH、兼容三种连接模式。
- Linux x64、Windows ARM64 与 Android ARM32 继续只要 CI 签名构建成功；本轮不宣称这些平台的活体验收。
- 活体验收结果未跑之前，不得填写 PASS/失败、设备名、安装包 SHA-256 或工作流 run ID。

## 1.6.1 维护范围

`1.6.1` 已打不可移动的 `v1.6.1` tag。分页漏字与正文加载重试已进入该 Draft。公开稳定版仍为 `1.5.1`。

- 沉浸阅读分页把正文裁进独立裁剪层，下一栏文字不再漏到当前页右侧。
- 小说正文加载失败时自动重试最多 3 次；登录失效或非法编号不重试，超过上限再显示错误。
- 在 Windows x64 与 Android ARM64 上，从公开 `1.5.1` 做覆盖升级活体验收；Android 须覆盖标准、ECH、兼容三种连接模式。
- Linux x64、Windows ARM64 与 Android ARM32 继续只要 CI 签名构建成功；本轮不宣称这些平台的活体验收。
- 活体验收结果未跑之前，不得填写 PASS/失败、设备名、安装包 SHA-256 或工作流 run ID。

## 1.6.2 维护范围

`1.6.2` 已打不可移动的 `v1.6.2` tag。长文按页渲染已进入该 Draft。公开稳定版仍为 `1.5.1`。

- 沉浸阅读按页测量并只挂载当前页，不再把整篇小说放进 CSS 分栏图层；二十万字级正文翻页不再因整篇排版卡顿。
- 在 Windows x64 与 Android ARM64 上，从公开 `1.5.1` 做覆盖升级活体验收；Android 须覆盖标准、ECH、兼容三种连接模式。
- Linux x64、Windows ARM64 与 Android ARM32 继续只要 CI 签名构建成功；本轮不宣称这些平台的活体验收。
- 活体验收结果未跑之前，不得填写 PASS/失败、设备名、安装包 SHA-256 或工作流 run ID。

## 1.6.3 维护范围

`1.6.3` 已打不可移动的 `v1.6.3` tag。列表自动加载、阅读页页码避让和可选音量键翻页已进入该 Draft。该 Draft 在所有平台显示音量键选项。公开稳定版仍为 `1.5.1`。

- 列表划到底自动加载下一页；加载失败时停止自动请求并显示重试。
- 阅读页页码不再挡住最后一行。
- 设置与阅读页可打开音量键翻页：音量下键下一页，音量上键上一页；仅在阅读页占用音量键，默认关闭。
- 2026-09-13 维护者确认 Android ARM64 真机上本轮新功能可用：列表自动加载、页码避让、可选音量键翻页。从公开 `1.5.1` 的覆盖升级、三种连接模式与 Windows x64 活体验收仍未按完整条记录。
- Linux x64、Windows ARM64 与 Android ARM32 继续只要 CI 签名构建成功；本轮不宣称这些平台的活体验收。

## 1.6.4 维护范围

`1.6.4` 已打不可移动的 `v1.6.4` tag。音量键翻页选项仅 Android 显示已进入该 Draft。Draft 已签名 10 附件，但无法从当前 `main` 走官方 Publish。公开稳定版仍为 `1.5.1`。

- 音量键翻页选项只出现在 Android 的设置与阅读页；Windows 与 Linux 不显示该选项。
- 活体验收未执行；`1.6.3` 的 Android 新功能确认不自动转记为本轮通过。

## 1.6.5 维护范围

`1.6.5` 是当前激活的源码版本。`v1.6.4` tag 不可移动，且当前 `main` 已包含 Publish 工作流跳过过时 Android SDK `tools` 包的修复，因此作为第三位修订发布。公开稳定版仍为 `1.5.1`。

- 用户可见内容与 `1.6.4` 相同：沉浸分页小说阅读、分页漏字/页码避让/长文按页渲染、正文加载重试、列表自动加载、音量键翻页选项仅 Android 显示。
- Publish 与 Draft 工作流跳过当前 runner 不再提供的过时 Android SDK `tools` 包，使签名 Draft 能从当前 `main` 复验并公开。
- 在 Windows x64 与 Android ARM64 上，从公开 `1.5.1` 做覆盖升级活体验收；Android 须覆盖标准、ECH、兼容三种连接模式。`1.6.3` 的 Android 新功能确认不自动转记为本轮通过。
- Linux x64、Windows ARM64 与 Android ARM32 继续只要 CI 签名构建成功；本轮不宣称这些平台的活体验收。
- 活体验收结果未跑之前，不得填写 PASS/失败、设备名、安装包 SHA-256 或工作流 run ID。

## 后续边界

- 投稿、个人资料编辑和通知写操作不做。
- 多账号、新平台、私信、直播、云同步和网络实验继续保留在[无排期备选计划](OPTIONAL_FEATURES_PLAN.md)。
- 每次只激活一个明确的版本范围；当前激活的是 `1.6.5`。不顺带扩展账号、平台或新的缓存接口。
