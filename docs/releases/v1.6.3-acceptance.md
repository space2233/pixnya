# PixNya 1.6.3 验收记录

> 当前状态：源码已打成 `1.6.3`，用于自动加载更多、阅读页页脚避让与音量键翻页的签名 Draft；公开 latest stable 为 `1.5.1`
> 当前升级基线：公开稳定版 `1.5.1`
> 候选源码：源码版本已打成 `1.6.3`；Draft 工作流在推送后触发
> Draft / Publish 工作流：未完成（不得预先填写 run ID）

`v1.6.0`、`v1.6.1` 与 `v1.6.2` tag 已分别指向沉浸阅读、分页裁剪/加载重试与长文按页渲染 Draft，仓库 tag ruleset 禁止移动。本轮以 `1.6.3` 发布列表自动加载、阅读页页码避让和可选音量键翻页。`1.5.1` 仍是公开 latest stable。活体验收未执行。

本轮 `1.6.3` 与 `1.4.3` 使用同一活体验收条：Windows x64 与 Android ARM64 从公开 `1.5.1` 覆盖升级；Android 须覆盖标准、ECH、兼容三种连接模式。下列设备结果均为未跑，不得改成 PASS/失败或补写设备名、安装包 SHA-256、工作流 run ID。

## 自动验证

- 本地完整 `npm run test:full`、五平台签名 Draft、tag 与 10 附件：尚未针对 `1.6.3` 完成。

## Windows x64

- 目标：从公开 `1.5.1` NSIS 覆盖安装到 `1.6.3` Draft，核对本机启动、版本注册、非敏感数据保留；打开长篇小说沉浸阅读页确认分页不漏字、页码不挡最后一行、翻页不卡顿；并确认列表划到底会自动加载下一页。
- 结果：未跑。

## Android ARM64

- 目标：从公开 `1.5.1` ARM64 APK 覆盖安装到 `1.6.3` Draft；核对本机安装、版本码、登录/设置/数据库/离线内容保留；覆盖标准、ECH、兼容三种连接模式；打开长篇小说沉浸阅读页确认分页不漏字、页码不挡最后一行、翻页不卡顿；打开设置中的音量键翻页后确认音量下/上可翻页且离开阅读页后音量键恢复调音量；并确认列表划到底会自动加载下一页。
- 结果：未跑。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：本轮只要求 `1.6.3` 签名 CI 构建成功，不宣称活体验收。
