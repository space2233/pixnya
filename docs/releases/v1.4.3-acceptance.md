# PixNya 1.4.3 验收记录

> 当前状态：阻止公开，等待 Android ARM64 活体验收
> 候选源码：`95e7bf74a7f9ab5cc7cbe13f460c29e2a8580705`
> Draft 工作流：GitHub Actions `32128825447`

未公开的 `1.4.2` Draft（源码 `4cb8ac6b1f1b3dcf8ca170d37c848bb611edbfd1`，GitHub Actions `32115481800` attempt 2）仅作为安全依赖、严格 ECH、五平台签名和升级流程的测试准备证据，不作为 `1.4.3` 正式验收结果。

## 自动验证

- 本地 `npm run test:full`：通过，272 个 Node 回归、Svelte 0 错误/0 警告、Rust fmt/Clippy/workspace tests 全部成功。
- Android Gradle/Maven：在线与离线锁图解析通过，343 个锁定组件；`extractReleaseAnnotations` 使用 Bouncy Castle `1.80.2`，ARM64/ARM32 runtime 图保持一致。
- OSV Scanner 2.5.0：Android ARM runtime 79 包零告警、零例外；84 条 build-tool finding 与限期精确基线一致。
- 严格 ECH：Windows 实网已验证 API、`i.pximg.net` 与 `s.pximg.net` 均接受 ECH；Android 仍需在 1.4.3 候选包复验。
- Windows 本地 Tauri debug 实包：生产前端与 Rust 编译成功；连接页、个人页、存储页、隐私页在手机/桌面视口截图复核通过，重复标题、登录状态、空间信息和日志按钮布局符合 1.4.3 规格。
- GitHub：preflight、Rust advisory、Windows x64/ARM64、Linux x64、Android ARM64/ARM32 和 Draft 汇总全部成功；tag 已原子绑定候选源码，10 个附件均为 uploaded 且带 SHA-256 digest。

## Windows x64

- 系统：Windows 11 专业版 x64，`10.0.26200`。
- 公开 `1.4.0` 与 `1.4.3` Draft x64 安装包均与 GitHub asset SHA-256 一致。
- Windows 11 x64 上安装并启动公开 `1.4.0`，再覆盖安装并启动 `1.4.3`；两次安装退出码均为 0，注册表与可执行文件版本均为 `1.4.3`，窗口正常打开。
- 非敏感 marker 完整保留；6 个关键记录无缺失，5 个哈希不变，唯一变化为 WebView 启动时更新的 `Crashpad/settings.dat`，业务数据库未变化。
- 安装后首页、连接页、个人页、存储页和隐私日志布局截图复核通过。Windows 基线未登录且没有现成离线作品，账号凭据与真实离线内容保留由 Android 主设备验收覆盖。

## Android ARM64

- Draft ARM64 APK 已离线验证：`versionName 1.4.3`、`versionCode 1004003`、`minSdk 29`、包名 `io.github.space2233.pixnya`、唯一 `arm64-v8a` ELF、大小和 SHA-256 均与签名清单及 GitHub asset 一致。
- `PENDING`：设备尚未连接 ADB；连接后从公开 `1.4.0` 覆盖安装 1.4.3 Draft，验证登录状态、设置、数据库、离线内容、字号和备份功能保留。
- `PENDING`：标准、ECH、兼容三模式的登录、API 和图片加载。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：签名 CI 构建成功，本轮不宣称活体验收。
