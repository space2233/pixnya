# PixNya 1.4.1 验收记录

> 当前状态：阻止公开，等待 Windows 登录态人工检查与 Android ARM64 真机验收
> 候选源码：`fb40c46`
> Draft 工作流：GitHub Actions `32035278025`，attempt 2

## 自动验证

- 本地 `npm run test:full`：通过，263 个 Node 回归、Svelte 0 错误/0 警告、Rust fmt/Clippy/workspace tests 全部成功。
- Android Gradle/Maven：在线与离线锁图解析通过，`extractReleaseAnnotations` 使用 Bouncy Castle `1.80.2`；APK runtime 79 包、OSV 零例外。
- GitHub：Windows x64/ARM64、Linux x64、Android ARM64/ARM32 均完成生产签名构建；Draft 汇总和 10 个附件验签成功。

## Windows x64

- 系统：Windows 11 专业版 x64，`10.0.26200`。
- `1.4.0` 与 `1.4.1` 安装包均先按 `SHA256SUMS.txt` 验证。
- 公开 `1.4.0` 静默安装成功并可启动；`1.4.1` Draft 覆盖安装退出码为 0，注册表和可执行文件版本均为 `1.4.1`，升级后可再次启动。
- 测试离线文件在覆盖安装后保留；`download-queue-v1.sqlite3` 升级前后 SHA-256 均为 `6ddee8e95e8bf4914643cd749d80d7a41e66d4e624ff9be3b8deb2508bd760cf`。
- 限制：升级前没有现成登录会话，尚未验证 refresh token、账号设置和界面内备份/恢复操作；测试 marker 已删除。

## Android ARM64

- `PENDING`：当前未连接设备，未执行 `1.4.0` → `1.4.1` 覆盖安装。
- `PENDING`：标准、ECH、兼容三模式的登录、API 和图片加载。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：签名构建通过，未做本轮活体验收。
