# PixNya 1.4.2 验收记录

> 当前状态：阻止公开，等待 1.4.2 签名 Draft 与 Windows x64、Android ARM64 活体验收
> 候选源码：`PENDING`
> Draft 工作流：`PENDING`

## 自动验证

- 本地 `npm run test:full`：通过，266 个 Node 回归、Svelte 0 错误/0 警告、Rust fmt/Clippy/workspace tests 全部成功。
- Android Gradle/Maven：在线与离线锁图解析通过，343 个锁定组件；`extractReleaseAnnotations` 使用 Bouncy Castle `1.80.2`，ARM64/ARM32 runtime 图保持一致。
- 严格 ECH：Windows 实网已验证 API、`i.pximg.net` 与 `s.pximg.net` 均接受 ECH；Android 仍需在 1.4.2 候选包复验。
- GitHub：`PENDING`，需完成 Windows x64/ARM64、Linux x64、Android ARM64/ARM32 生产签名构建及 10 个附件验签。

## Windows x64

- 系统：Windows 11 专业版 x64，`10.0.26200`。
- 先前 `1.4.0` → 未公开 `1.4.1` Draft 的安装演练成功且测试离线文件、下载队列数据库保持不变；该结果仅作准备证据，不替代 1.4.2 验收。
- `PENDING`：校验公开 `1.4.0` 与 `1.4.2` Draft 安装包，完成覆盖安装、重启、登录状态、设置、数据库、离线内容和备份功能检查。

## Android ARM64

- `PENDING`：执行 `1.4.0` → `1.4.2` 覆盖安装；若现有主设备无法安全降级到基线，则改用独立 ARM64 测试设备，不能以旧 Draft 验收替代。
- `PENDING`：标准、ECH、兼容三模式的登录、API 和图片加载。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：`PENDING` 签名 CI 构建，本轮不承诺活体验收。
