# PixNya 1.6.4 验收记录

> 当前状态：源码已打成 `1.6.4`，签名 Draft 已生成；公开 latest stable 为 `1.5.1`
> 当前升级基线：公开稳定版 `1.5.1`
> 候选源码：`17dbcf4e162abcc7e6380adeb9954eebd1cdfb91`
> Draft / Publish 工作流：Draft GitHub Actions `34740686034` 已完成；未 Publish。tag `v1.6.4` 不可移动；当前 `main` 已领先该 tag 一提交，官方 Publish 无法从当前 `main` 复验该 Draft。

`v1.6.0`、`v1.6.1`、`v1.6.2` 与 `v1.6.3` tag 已分别指向沉浸阅读、分页裁剪/加载重试、长文按页渲染与自动加载/音量键 Draft，仓库 tag ruleset 禁止移动。本轮以 `1.6.4` 发布音量键翻页选项仅 Android 显示。`v1.6.4` tag 指向上述候选源码。`1.5.1` 仍是公开 latest stable。活体验收未执行。

上一轮 `1.6.3` Draft 上，维护者确认过 Android ARM64 真机上列表自动加载、页码避让和音量键翻页可用；该结果不自动转记为 `1.6.4` 通过。本轮 `1.6.4` 与 `1.4.3` 使用同一完整活体验收条：Windows x64 与 Android ARM64 从公开 `1.5.1` 覆盖升级；Android 须覆盖标准、ECH、兼容三种连接模式。下列设备结果均为未跑，不得改成 PASS/失败或补写设备名、安装包 SHA-256、工作流 run ID。

## 自动验证

- Draft 工作流 `34740686034` 完成五平台签名、tag `v1.6.4` 与 10 附件。
- 官方 Publish 无法从当前 `main` 复验：`release_workflow_commit` 须等于 Publish 时的 `GITHUB_SHA`，而当前 `main` 已领先 `v1.6.4` 一提交。

## Windows x64

- 目标：从公开 `1.5.1` NSIS 覆盖安装到 `1.6.4` Draft，核对本机启动、版本注册、非敏感数据保留；打开长篇小说沉浸阅读页确认分页不漏字、页码不挡最后一行、翻页不卡顿；确认列表划到底会自动加载下一页；并确认设置与阅读页没有音量键翻页选项。
- 结果：未跑。

## Android ARM64

- 目标：从公开 `1.5.1` ARM64 APK 覆盖安装到 `1.6.4` Draft；核对本机安装、版本码、登录/设置/数据库/离线内容保留；覆盖标准、ECH、兼容三种连接模式；打开长篇小说沉浸阅读页确认分页不漏字、页码不挡最后一行、翻页不卡顿；打开设置中的音量键翻页后确认音量下/上可翻页且离开阅读页后音量键恢复调音量；并确认列表划到底会自动加载下一页。
- 结果：未跑。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：`1.6.4` 签名 CI 构建成功，不宣称活体验收。Linux 安装包应与 Windows 一样不显示音量键翻页选项，但不做活体验收记录。
