# PixNya 1.6.0 验收记录

> 当前状态：源码已打成 `1.6.0`，用于沉浸小说阅读的签名 Draft；公开 latest stable 为 `1.5.1`
> 当前升级基线：公开稳定版 `1.5.1`
> 候选源码：源码版本已打成 `1.6.0`；Draft 工作流在推送后触发
> Draft / Publish 工作流：未完成（不得预先填写 run ID）

`1.5.1` 已公开为 latest stable，固定候选 `302e117e234aff16658c1a6f82c707c1697cd40e`；Draft GitHub Actions `33730303676` 完成五平台构建、签名与 10 附件汇总。该 Release 曾作为 Pre-release 公开；官方 Publish 工作流只接受 Draft，无法再复验，维护者随后直接将其标为 latest。活体验收未执行。

本轮 `1.6.0` 与 `1.4.3` 使用同一活体验收条：Windows x64 与 Android ARM64 从公开 `1.5.1` 覆盖升级；Android 须覆盖标准、ECH、兼容三种连接模式。下列设备结果均为未跑，不得改成 PASS/失败或补写设备名、安装包 SHA-256、工作流 run ID。

## 自动验证

- 本地完整 `npm run test:full`、五平台签名 Draft、tag 与 10 附件：尚未针对 `1.6.0` 完成。
- 2026-09-12 Draft preflight 因 Android 构建工具 OSV 基线从 84 条增至 88 条失败；随后将 `GHSA-c4c3-7fpv-j4q5` 与 `GHSA-fccg-mwvh-qqg4` 记入精确、限期、非 runtime 例外。ARM runtime 仍为零例外。
- 2026-09-12 `cargo-deny` 因撤回的 GTK3 停止维护公告报 unused ignore；已从 `deny.toml` 删除 `RUSTSEC-2024-0411`–`0420`。

## Windows x64

- 目标：从公开 `1.5.1` NSIS 覆盖安装到 `1.6.0` Draft，核对本机启动、版本注册、非敏感数据保留，并打开小说沉浸阅读页。
- 结果：未跑。

## Android ARM64

- 目标：从公开 `1.5.1` ARM64 APK 覆盖安装到 `1.6.0` Draft；核对本机安装、版本码、登录/设置/数据库/离线内容保留；覆盖标准、ECH、兼容三种连接模式；并打开小说沉浸阅读页。
- 结果：未跑。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：本轮只要求 `1.6.0` 签名 CI 构建成功，不宣称活体验收。
