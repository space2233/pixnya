# PixNya 1.5.1 验收记录

> 当前状态：维护补丁准备中；公开 latest stable 仍为 `1.5.0`
> 当前升级基线：公开稳定版 `1.5.0`
> 候选源码：源码版本已打成 `1.5.1`，尚未触发 `1.5.1` 签名 Draft
> Draft / Publish 工作流：未运行（不得预先填写 run ID）

`1.5.0` 已公开为 latest stable，固定候选 `b5de41654e84389542ee8f1c3f7259c224f2d935`；Draft GitHub Actions `33059441874` 完成五平台构建、签名与 10 附件汇总，Publish `33162282043` 于 2026-08-28T10:10:21Z 复验后公开。维护者在首个稳定版清单与 `1.5.0` 发布记录中写明本轮不做人机活体验收。未公开的 `1.4.4` Draft Release 已删除；tag `v1.4.4`（`c433fc5e0ff2a3eff999d7b5a384cf6c4d6006c3`）因仓库不可删除 ruleset 保持原 SHA，维护内容已包含在 `1.5.0` 中。

上一轮已完成活体验收的是 `1.4.3`：Windows x64 与 Android ARM64 从公开 `1.4.0` 覆盖升级。Linux x64、Windows ARM64 与 Android ARM32 在 `1.4.3` 与 `1.5.0` 均只有签名 CI 构建，没有活体验收记录。

本轮 `1.5.1` 与 `1.4.3` 使用同一活体验收条：Windows x64 与 Android ARM64 从公开 `1.5.0` 覆盖升级；Android 须覆盖标准、ECH、兼容三种连接模式。下列设备结果均为未跑，不得改成 PASS/失败或补写设备名、安装包 SHA-256、工作流 run ID。

## 自动验证

- 2026-09-02 使用 checksum 固定的 OSV Scanner 2.5.0 重新扫描三套 Android Gradle 锁图：观察到 84 条 build-only finding，与已提交基线的 advisory、Maven 坐标、版本、severity、scope 和 fixedVersions 完全一致；无新增、无消失。`scripts/android-build-tool-osv-baseline-regression.test.mjs`、`scripts/rust-advisory-policy-regression.test.mjs` 与 `scripts/release-boundaries-regression.test.mjs` 均通过。
- Android ARM runtime 策略不变：零例外、零 ignore。构建工具例外全部于 2026-09-02 复核，到期日续至 2026-10-02。Toolchain 仍为 Tauri 2.11.5、AGP 8.11.0、Kotlin Gradle Plugin 1.9.25、Gradle 8.14.3、JDK 17。
- RustSec 临时例外（17 条）于 2026-09-02 复核：锁图仍为 Tauri 2.11.5 / `glib 0.18.5` / `urlpattern 0.3.0`；GTK4 与已发布的 urlpattern 0.6 迁移仍未进入当前 Tauri 发行版。`review-by` 续至 2026-10-02。未升级 Tauri。
- 本地完整 `npm run test:full`、五平台签名 Draft、tag 与 10 附件：尚未针对 `1.5.1` 运行。

## Windows x64

- 目标：从公开 `1.5.0` NSIS 覆盖安装到 `1.5.1` Draft，核对本机启动、版本注册、非敏感数据保留。
- 结果：未跑。

## Android ARM64

- 目标：从公开 `1.5.0` ARM64 APK 覆盖安装到 `1.5.1` Draft；核对本机安装、版本码、登录/设置/数据库/离线内容保留；并覆盖标准、ECH、兼容三种连接模式的登录、API 与图片加载冒烟。
- 结果：未跑。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：本轮只要求 `1.5.1` 签名 CI 构建成功，不宣称活体验收。`1.4.3` 与 `1.5.0` 同样没有这些平台的活体验收记录。

## 发布后清理

- 尚未创建 `1.5.1` Draft 或 tag；无清理动作。
