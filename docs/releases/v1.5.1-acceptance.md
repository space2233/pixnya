# PixNya 1.5.1 验收记录

> 当前状态：已公开为 latest stable；活体验收未执行
> 当前升级基线：公开稳定版 `1.5.0`
> 候选源码：`302e117e234aff16658c1a6f82c707c1697cd40e`
> Draft 工作流：`33730303676`（2026-09-03）；Release ID `381847287`

`1.5.1` 已公开为 latest stable。Draft GitHub Actions `33730303676` 完成五平台构建、签名与 10 附件汇总。该 Release 曾于 2026-09-12 作为 Pre-release 公开（不抢 `1.5.0` latest）；官方 Publish 工作流只接受 Draft，无法再复验，维护者随后直接将其标为 latest。未公开的 `1.4.4` Draft Release 已删除；tag `v1.4.4`（`c433fc5e0ff2a3eff999d7b5a384cf6c4d6006c3`）因仓库不可删除 ruleset 保持原 SHA，维护内容已包含在 `1.5.0` 中。

上一轮已完成活体验收的是 `1.4.3`：Windows x64 与 Android ARM64 从公开 `1.4.0` 覆盖升级。Linux x64、Windows ARM64 与 Android ARM32 在 `1.4.3`、`1.5.0` 与 `1.5.1` 均只有签名 CI 构建，没有活体验收记录。

本轮 `1.5.1` 与 `1.4.3` 使用同一活体验收条：Windows x64 与 Android ARM64 从公开 `1.5.0` 覆盖升级；Android 须覆盖标准、ECH、兼容三种连接模式。下列设备结果均为未跑，不得改成 PASS/失败或补写设备名、安装包 SHA-256。

## 自动验证

- 2026-09-02 使用 checksum 固定的 OSV Scanner 2.5.0 重新扫描三套 Android Gradle 锁图：观察到 84 条 build-only finding，与已提交基线的 advisory、Maven 坐标、版本、severity、scope 和 fixedVersions 完全一致；无新增、无消失。`scripts/android-build-tool-osv-baseline-regression.test.mjs`、`scripts/rust-advisory-policy-regression.test.mjs` 与 `scripts/release-boundaries-regression.test.mjs` 均通过。
- Android ARM runtime 策略不变：零例外、零 ignore。构建工具例外全部于 2026-09-02 复核，到期日续至 2026-10-02。Toolchain 仍为 Tauri 2.11.5、AGP 8.11.0、Kotlin Gradle Plugin 1.9.25、Gradle 8.14.3、JDK 17。
- RustSec 临时例外（17 条）于 2026-09-02 复核：锁图仍为 Tauri 2.11.5 / `glib 0.18.5` / `urlpattern 0.3.0`；GTK4 与已发布的 urlpattern 0.6 迁移仍未进入当前 Tauri 发行版。`review-by` 续至 2026-10-02。未升级 Tauri。
- 五平台签名 Draft 与 tag：GitHub Actions `33730303676` 已完成；10 个公开附件已绑定 `v1.5.1`。

## Windows x64

- 目标：从公开 `1.5.0` NSIS 覆盖安装到 `1.5.1`，核对本机启动、版本注册、非敏感数据保留。
- 结果：未跑。

## Android ARM64

- 目标：从公开 `1.5.0` ARM64 APK 覆盖安装到 `1.5.1`；核对本机安装、版本码、登录/设置/数据库/离线内容保留；并覆盖标准、ECH、兼容三种连接模式的登录、API 与图片加载冒烟。
- 结果：未跑。

## 仅 CI 平台

- Windows ARM64、Linux x64、Android ARM32：本轮只要求 `1.5.1` 签名 CI 构建成功，不宣称活体验收。`1.4.3` 与 `1.5.0` 同样没有这些平台的活体验收记录。

## 发布后清理

- `v1.5.1` 已公开为 latest stable；无需删除本版 Draft。未公开的 `1.4.4` Draft Release 已删除。
