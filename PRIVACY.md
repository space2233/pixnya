# PixNya 隐私说明

> 适用于 PixNya 首个正式版候选及之后的开源侧载版本。

PixNya 是本地优先的非官方 Pixiv 客户端。项目维护者不运营账号、内容或流量中转服务；应用默认不收集遥测、不上传崩溃报告，也不建立独立的用户账号系统。

## 本机保存的数据

为提供登录、离线和个性化界面，PixNya 可能在设备本机保存：

- Pixiv refresh token：Windows/Linux 使用系统凭据库，Android 使用 AndroidKeyStore AES-GCM 加密；access token 只保存在运行内存中。
- 登录 WebView Cookie：保存在对应系统 WebView 的应用隔离存储中。
- 设置与提示确认状态：包括语言、主题、R18 显示、连接偏好、更新偏好和风险提示选择。
- 搜索与浏览历史、小说阅读进度。
- 下载队列、离线作品/小说/Ugoira、收藏夹与本机标签。
- 有容量和保留期限的图片缓存与脱敏诊断日志。

这些数据不会由 PixNya 项目维护者自动接收。用户主动导出文件、截图或诊断日志后，应自行决定发送对象并检查其中内容。

## 网络请求

PixNya 只因用户使用功能而连接以下服务：

- Pixiv 官方登录页面、App API 与图片 CDN，用于登录、浏览、互动和媒体加载。
- 可信 DNS over HTTPS 服务，仅在严格 ECH 路线中查询连接配置。
- GitHub Releases 或未来明确公布的同等公开源，仅用于检查和下载 PixNya 更新。

更新请求使用系统网络和经过验证的 HTTPS，不会携带 Pixiv token、Cookie、账号 ID 或浏览历史。PixNya 不在正式客户端内置私人 GitHub 访问令牌。

## 三种连接模式

- 标准模式使用系统 DNS、代理与证书验证。
- 严格 ECH 模式要求 Rust 网络层确认 ECH 被接受，否则失败关闭。
- 兼容模式以及 Android 的非标准网页登录桥可能关闭部分上游 TLS 身份验证，只能由用户主动选择；1.1 起风险弹窗默认不显示，可在设置中恢复。

低安全路线可能使网络中间人读取或修改登录凭据、令牌和内容。不显示警告不会降低风险；该路线不会被自动保存为默认模式，也不会在安全路线失败后静默启用。

## 数据清除与保留

“设置 → 存储与隐私”提供强确认的本机数据清除。清除范围包括安全令牌、登录 Cookie、队列、离线资料、缓存、历史、阅读进度、日志与界面设置；应用会逐项报告失败。用户已经导出到自选目录的文件不属于应用私有存储，不会被自动删除。

卸载应用通常会删除应用私有数据，但系统凭据库、系统 WebView 或用户导出目录的具体行为取决于操作系统。需要彻底清除时，应先在应用内执行清除，再按系统提供的应用数据或凭据管理功能复查。

## 第三方内容与账号

Pixiv 返回的账号资料、作品、标签、评论和小说内容由 Pixiv 及对应权利人处理。用户使用 PixNya 时仍受 Pixiv 条款、所在地法律和作品版权约束。本说明只描述 PixNya 自身的数据行为，不替代 Pixiv 的隐私政策。

## 变更

隐私行为发生实质变化时，项目会在源码、Release 说明和本文件中同步说明。任何未来遥测或崩溃上传都必须默认关闭、明确列出字段，并先取得用户主动同意。
