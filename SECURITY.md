# PixNya 安全政策

## 支持范围

首个正式版发布后，项目只为最新稳定版本提供安全修复。当前 `0.x` 开发版本用于候选验证，不承诺长期安全支持。调试包、临时构建和修改版不属于官方发布产物。

## 私密报告漏洞

请优先在 GitHub 仓库的 **Security → Report a vulnerability** 使用 Private Vulnerability Reporting。不要在公开 Issue、截图或日志中披露以下内容：

- access token、refresh token、OAuth code、Cookie、账号 ID 或邮箱；
- 登录页面内容、二步验证码或低安全连接捕获的数据；
- Android keystore、Tauri updater 私钥、更新清单私钥或任何构建 Secret；
- 能在修复前直接利用的完整攻击步骤。

如果仓库暂时未开放私密报告入口，可创建一条不含技术细节和个人数据的公开 Issue，请维护者建立私密沟通渠道。维护者确认安全问题后，会在不暴露报告者隐私的前提下协调修复与发布。

报告中建议包含：受影响版本与平台、是否来自官方 Release、最小复现条件、预期/实际安全边界，以及经过脱敏的日志。请不要使用真实受害者账号、批量请求或破坏性测试。

## 重点安全边界

- PixNya 没有账号密码输入框；密码只应在 Pixiv 官方登录页面输入。
- token 不能进入前端 API、URL、普通日志、诊断导出或更新请求。
- 低安全连接必须由用户主动选择，不能成为默认值或静默回退目标；1.1 起风险弹窗默认不显示，但设置页始终标明风险并允许恢复弹窗。
- 更新只接受固定 HTTPS 来源、更高稳定版本、正确平台/ABI，以及与内置公钥或证书摘要匹配的签名产物。
- Android 安装包的 application ID 和长期 Release 证书必须保持不变；桌面 updater 与 Android 清单使用独立长期密钥。
- 任何签名、证书、哈希、包名、版本或 ABI 验证失败都必须 fail closed。

## 发布密钥事故

怀疑私钥或 keystore 泄露时应立即停止创建 Release，撤销受影响的 GitHub Secrets，保留审计证据并公告受影响范围。Android Release 证书泄露可能无法通过普通更新安全迁移；不得用远程配置关闭签名校验。密钥轮换方案必须作为独立安全事件评审。

## 非安全问题

普通 UI、兼容性、上游 API 变化或不包含敏感数据的崩溃请使用常规 Issue。低安全连接模式必须主动启用；其固有的中间人风险本身不是漏洞，但静默启用或风险范围扩大属于安全问题。
